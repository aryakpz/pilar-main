export enum ButtonVariant {
    DARK = "dark",
    LIGHT = "light"
}

export enum ButtonType {
    SUBMIT = "submit",
    BUTTON = "button"
}

export enum LanguageType{
    ENG="English",
    DUT="Dutch"
}