export enum ButtonVariant {
    DARK = "dark",
    LIGHT = "light"
}

export enum ButtonType {
    SUBMIT = "submit",
    BUTTON = "button"
}

export enum Language {
    ENG = "English",
    DUT = "Dutch"
}